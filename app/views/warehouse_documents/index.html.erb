<% provide(:title, "Depo doracake") %>
<% if logged_in? %>
  <div class="row">
    <aside class="col-md-4">
      <section class="user_info">
        <%= render 'shared/user_info' %>
      </section>
      <section class="pending_info">
        <%= render 'shared/pending_info' %>
      </section>
      <section class="perkujtimet_info">
        <%= render 'shared/perkujtimet' %>
      </section>
      
    </aside>
    
    
    <h1>Depo doracake</h1>
    <div class="centered">
      <p><%= link_to "Pranim malli", warehouse_documents_pranim_malli_path, class: "btn btn-primary btn-lg butoni250" %></p>
      <p><%= link_to "Dalje nga depo", dalje_depo_path, class: "btn btn-primary btn-lg butoni250" %></p>
      <p><%= link_to "Artikujt", items_path, class: "btn btn-primary btn-lg butoni250" %></p>
      <p><%= link_to "Furnitoret", suppliers_path, class: "btn btn-primary btn-lg butoni250" %></p>
      <p><%= link_to "Raportet", warehouse_documents_pranim_malli_path, class: "btn btn-primary btn-lg butoni250" %></p>
    </div>
    
  </div>
<% else %>
  <h3>Kycuni, ju lutem!</h3>  
<% end %>  